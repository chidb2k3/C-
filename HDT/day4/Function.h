#ifndef FUNCTION_H
#define FUNCTION_H
#include <vector>
#include "Pet.h"
#include <vector>
using namespace std;
class Function
{
private:
    
public:
    Function();
    vector<Pet*> createPets();
    // void doFoodForPets(vector<Pet*>,vector<string>)
    // void makeSoundForPets(vector<Pet*>)
   
};
#endif
